@model IEnumerable<TeamManager.Models.CoachNotes>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New Note", "Create")
</p>
<table class="table">
    <tr>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.coachNotes }) |
            @Html.ActionLink("Details", "Details", new { id=item.coachNotes }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.coachNotes })
        </td>
    </tr>
}

</table>
